/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // Ensure we're not using any experimental features that might cause issues
    experimental: {
      // Disable any experimental features
    },
  }
  
  export default nextConfig
  